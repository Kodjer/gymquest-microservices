# CI/CD Pipeline для GymQuest Microservices

## Что делает pipeline:

### 1. **Test Job** (Тестирование)
- Запускается для каждого из 6 сервисов параллельно
- Устанавливает Node.js 22
- Устанавливает зависимости через `npm ci`
- Запускает юнит-тесты (`npm test`)
- Использует секреты SUPABASE_URL и SUPABASE_ANON_KEY

### 2. **Build Job** (Сборка)
- Запускается только после успешного прохождения тестов
- Только для push в ветку `main`
- Собирает Docker образы для всех 6 сервисов
- Проверяет что docker-compose работает корректно

### 3. **Deploy Job** (Деплой)
- Запускается после успешной сборки
- Выводит сообщение о готовности к деплою
- Можно добавить реальный деплой на сервер

## Как настроить:

1. Перейди в Settings → Secrets and variables → Actions
2. Добавь секреты:
   - `SUPABASE_URL` - URL твоего Supabase проекта
   - `SUPABASE_ANON_KEY` - Анонимный ключ Supabase

## Когда запускается:

- ✅ При каждом push в ветку `main`
- ✅ При создании Pull Request в `main`

## Статусы:

- 🟢 Green check - все тесты прошли
- 🔴 Red X - тесты упали
- 🟡 Yellow - pipeline выполняется
